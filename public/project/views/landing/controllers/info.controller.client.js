(function () {
    angular
        .module("projectApp")
        .controller("InfoController", InfoController);

    function InfoController($location, $rootScope) {
        var model = this;

        function init() {

        }

        init();

    }

})();
