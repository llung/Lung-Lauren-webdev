(function () {
    angular
        .module("projectApp")
        .controller("LandingController", LandingController);

    function LandingController($location, $rootScope) {
        var model = this;

        function init() {

        }

        init();

    }

})();
