(function () {
    angular
        .module("projectApp")
        .controller("RegisterController", RegisterController);

    function RegisterController($location, $rootScope) {
        var model = this;

        function init() {

        }

        init();

    }

})();
