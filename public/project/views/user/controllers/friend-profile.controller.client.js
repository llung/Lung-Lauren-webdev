(function () {
    angular
        .module("projectApp")
        .controller("FriendsController", FriendsController);

    function FriendsController($location, $rootScope) {
        var model = this;

        function init() {

        }

        init();

    }

})();
